# 提升 val bpb 的三个方向（临时工作文档）

> 创建时间：2026-09-13
> 状态：代码 bug 已全部修复，当前瓶颈是**数据规模**，不是代码。
> **更新（2026-09-15）：A、B、C 三个方向均已实测完毕，结论见第 7 节 —— 当前最优 val bpb = 2.362
> （A+B2+C：4.15 MB 语料 + 1.84M 参数 + 正则），已达成 A 方向目标 `< 2.6`，但离"生成通顺"所需的 `< 2.0` 仍有距离。**
> 本文档只讲"怎么改、改哪里、跑什么命令"，不含原理解释（原理见 `docs/`）。

---

## 0. 背景与现状

### 已经做完的事

flash attention 反向的两个 bug 已修复并验证（详见文末附录），修复后训练行为完全改变：

| 指标 | 修复前 | 修复后 |
|------|--------|--------|
| 梯度范数（step 1→1500） | 0.67 → **5,789,792**（爆炸） | 0.55 → **1.20**（稳定） |
| train loss 最低 | 卡死在 ~6.9 | **0.61** |
| best val bpb | 2.975 | **2.884** |

### 当前瓶颈

```
训练集 60,566 token
模型   4,208,128 参数        → 参数/token = 69 倍
```

模型约 500 步就把训练集背完了（train bpb 降到 0.248 ≈ 完美记忆），之后 val 一路恶化：

| step | train bpb | val bpb |
|------|-----------|---------|
| 500 | 1.872 | **2.884** ← 最优 |
| 800 | 1.157 | 3.106 |
| 1200 | 0.453 | 3.332 |
| 1500 | 0.248 | 3.455 |

**这是纯粹的过拟合，不是 bug。** 三个方向分别针对它的不同侧面。

### 参照系（bits/byte，越低越好）

| 基线 | bpb |
|------|-----|
| unigram | 5.969 |
| bigram | 3.887 |
| gzip | 3.150 |
| 当前模型最好（step 500） | **2.884** |
| 目标（生成才可能通顺） | **< 2.0** |

---

## 1. 通用操作（三个方向都要用）

### 1.1 参数数量估算公式

改模型尺寸前先估算参数量，避免改完才发现太大/太小：

```
params ≈ vocab × d  +  n_layer × (12·d² + 13·d)  +  2·d

  vocab   = 词表大小（bpe_vocab）
  d       = n_embd
  12·d²   = 4 个注意力投影 (c_q/c_k/c_v/c_proj) + MLP 的两个投影
  13·d    = 各处 bias + LayerNorm 的 γ/β
```

验证（当前配置 vocab=4096, d=256, n_layer=4）：
`4096×256 + 4×(12×65536 + 13×256) + 512 = 1,048,576 + 3,159,040 + 512 = 4,208,128` ✅ 与启动横幅一致。

### 1.2 编译

```powershell
cd D:\Code\Rust\llm_from_scratch
cargo build --release --features gpu
```

### 1.3 启动训练（**必须脱离进程树**）

> **⚠️ 2026-09-15 修正**：原方案用 `cmd.exe /c`，但该写法已被本机安全策略禁用
> （报错 `The use of 'cmd /c' ... is blocked on Windows for safety`）。
> 另外，**直接在当前 shell 里后台运行会被工具的任务对象连带杀掉**——实测一个已跑到 step 878 的训练
> 进程在某次终端回收时被终止（终止码 `0x40010004`），12 小时无人察觉。
> 下面是用 `Start-Process` + WMI 实测通过的替代方案。

```powershell
$base = 'D:\Code\Rust\llm_from_scratch'
$exe  = "$base\target\release\llm_from_scratch.exe"

# 先自检：确认这套启动链路能正确写 UTF-8 日志（应该输出「验证通过」）
$inner = "Start-Process -FilePath '" + $exe + "' -ArgumentList '--help' -RedirectStandardOutput '" + $base + "\_probe.txt' -NoNewWindow -Wait"
$c = 'powershell.exe -NoProfile -Command "' + $inner + '"'
Invoke-CimMethod -ClassName Win32_Process -MethodName Create -Arguments @{CommandLine=$c} | Out-Null
Start-Sleep -Seconds 10
$b = [IO.File]::ReadAllBytes("$base\_probe.txt")[0..3]
if (($b | ForEach-Object { $_.ToString('X2') }) -join ' ' -eq 'E4 B8 80 E4') { Write-Output '验证通过' } else { Write-Output '验证失败，勿启动训练' }
Remove-Item "$base\_probe.txt"

# 正式启动（train 换成需要跑的场景；--resume 可省）
$inner = "Start-Process -FilePath '" + $exe + "' -ArgumentList 'train --config config.json --resume checkpoints/latest.ckpt' -RedirectStandardOutput '" + $base + "\logs\train_v5.log' -RedirectStandardError '" + $base + "\logs\train_v5.err.log' -WorkingDirectory '" + $base + "' -NoNewWindow -Wait"
$c = 'powershell.exe -NoProfile -Command "' + $inner + '"'
$r = Invoke-CimMethod -ClassName Win32_Process -MethodName Create -Arguments @{CommandLine=$c}
Write-Output "wrapper PID=$($r.ProcessId)"
```

> **为什么绕这一圈（踩坑记录）**：在 WMI 拉起的 PowerShell 里，
> 1. `& exe ... | Out-File -Encoding utf8` **会静默失败**（文件根本不生成）——外部 exe 的管道在此上下文不可靠；
> 2. 换成 `& exe ... > log` 虽然能写，但 PS 5.1 的 `>` 默认产出 **UTF-16LE**（前两字节 `FF FE`），日志全乱码；
> 3. 只有 `Start-Process -RedirectStandardOutput` 实测写出**UTF-8 无 BOM**（前 4 字节 `E4 B8 80 E4`），可用。
>
> **注意 `-RedirectStandardOutput` 是覆盖写，不能追加**。续训请写到新文件（如 `logs\train_a2.log`），
> 否则会冲掉原日志。

> **⚠️ 2026-09-16 补充**：即使按上面的方式脱离进程树，**仍会被外部终止**——
> A2 那次跑到 step 2845（10.5 小时）后日志戛然而止，`logs\train_a2.err.log` 为空、事件日志无记录、无 `[done]`，
> 属于外部 `TerminateProcess`，与本次改动无关。原因未定位（疑似 IDE/会话回收）。
> **兜底办法就是 `--resume` + `latest.ckpt`**：`eval_every=200` 意味着最多丢 200 步，
> 续训命令见 8.7。建议长时间训练前先确认机器不会重启/休眠。

### 1.4 终止训练（**按进程名杀，不要按 PID**）

```powershell
Get-Process | Where-Object { $_.ProcessName -like '*llm_from*' } | Stop-Process -Force
```

> **踩过的坑**：训练进程结构是 `cmd.exe` → `llm_from_scratch.exe`。
> 只杀 `cmd` 的 PID 会留下**僵尸 exe 继续训练**——曾遗留 6.5 小时，一边发散一边和新训练抢 GPU、
> 还往同一个 `checkpoints/` 目录写文件，导致新训练速度慢 1.7 倍。
> **按进程名杀可以同时覆盖父子进程。**

### 1.5 监控（含 bpb 自动换算）

```powershell
# 评估行 + bpb
Get-Content logs\train_v5.log -Encoding UTF8 | ForEach-Object {
  if ($_ -match '^\s*step\s+(\d+) \| lr ([\d.]+) \| loss ([\d.]+) \| val ([\d.]+) \(ppl ([\d.]+)\)') {
    Write-Output ("step {0,5} | loss {1} | val {2} | ppl {3} | bpb {4:N3}" -f `
      $Matches[1],$Matches[3],$Matches[4],$Matches[5],([double]$Matches[4]*0.40412))
  }
}
```

```powershell
# 梯度趋势（每 150 步窗口）——判断是否还在正常范围
$rows = @()
Get-Content logs\train_v5.log -Encoding UTF8 | ForEach-Object {
  if ($_ -match '^\[train\] step (\d+)/\d+ \| loss ([\d.]+) \| grad ([\d.e+]+)\*?') {
    $rows += [pscustomobject]@{ step=[int]$Matches[1]; loss=[double]$Matches[2]; grad=[double]$Matches[3] }
  }
}
for ($w=0; $w -lt 20; $w++) {
  $sel = $rows | Where-Object { $_.step -ge $w*150+1 -and $_.step -le $w*150+150 }
  if ($sel.Count -gt 0) {
    Write-Output ("step {0,4}-{1,4}: loss均值={2:N3}  grad峰值={3,10:N2}" -f `
      ($w*150+1),($w*150+150),($sel|Measure-Object loss -Average).Average,($sel|Measure-Object grad -Maximum).Maximum)
  }
}
```

> **读日志必须加 `-Encoding UTF8`**，否则中文乱码（程序输出 UTF-8，PowerShell 默认按 GBK 读）。

### 1.6 bpb 换算公式

```
bpb = val_loss × 系数        # 系数见下表，随语料/词表变化
```

推导：`bpb = loss_nats_per_token / ln(2) × n_tokens / n_bytes`

| 语料 / 词表 | n_tokens | n_bytes | 系数 | 备注 |
|---|---|---|---|---|
| 240 KB / 4096 | 67,296 | 240,251 | **0.40412** | 旧基线（第 7.1~7.6 节） |
| 4.15 MB / 8192 | 1,058,589 | 4,152,560 | **0.3678** | 方向 A（第 7.8 节） |
| 13.37 MB / 8192（中文古典） | 3,304,290 | 13,373,559 | **0.35646** | 方向 A2（第 8 节） |

计算过程：`(n_tokens / n_bytes) / ln(2)` → `(67296/240251)/0.6931 = 0.40412`；
`(1058589/4152560)/0.6931 = 0.3678`；`(3304290/13373559)/0.6931 = 0.35646`。

> **⚠️ 语料或词表一旦改变，这个系数就失效**，必须按上表重算，否则新旧 bpb **不可比**。
> n_tokens 见启动横幅"语料 N tokens"，n_bytes 用 `Get-ChildItem data\corpus -Filter *.txt | Measure-Object Length -Sum`。

> **为什么不能直接比 loss**：不同词表的随机基线不同（ln512=6.24，ln4096=8.32）。
> 词表从 512 换到 4096 时 loss 从 5.10"涨"到 7.14，但模型实际变好了 33%。
> **跨词表比较只能用 bpb。**

### 1.7 生成验证

```powershell
target\release\llm_from_scratch.exe generate --ckpt checkpoints/best.ckpt --prompt '人工智能' --max-new 60 --temperature 0.5
```

> 永远用 `best.ckpt`（验证集最优），**不要用 `latest.ckpt` / `final.ckpt`**——它们通常是过拟合后的劣化点。

---

## 2. 方向 A：扩大语料（**收益最大，优先做**）

### 2.1 目标量

过拟合的根本原因是参数/token = 69。要降到不过拟合，需要约 62 倍语料：

| 语料规模 | 约合 token | 参数/token（模型不变） |
|---------|-----------|---------------------|
| 当前 240 KB | 67,296 | 69 |
| 2.4 MB（×10） | ~672,000 | 6.9 |
| 15 MB（×62） | ~4,200,000 | 1.0 ← 基本不过拟合 |

**建议至少 ×10（2.4 MB）**，能明显缓解；×60 才能真正解决。

### 2.2 改哪里

**① 放语料文件：`data/corpus/`**

代码会自动加载该目录下**所有 `.txt`**，无需改代码：

- 位置：`src/data.rs:31-60` `load_text()`
- 行为：目录下所有 `*.txt` **按路径排序**后，用 `"\n"` 拼接成一个字符串
- 编码必须是 **UTF-8**
- 建议按现有命名习惯：`zh_article_xxx.txt` / `en_article_xxx.txt`，排序后同语言的会聚在一起

```powershell
# 拷贝新语料到语料目录（示例）
Copy-Item 'D:\我的文本\*.txt' 'D:\Code\Rust\llm_from_scratch\data\corpus\'

# 确认总量
$s = (Get-ChildItem 'D:\Code\Rust\llm_from_scratch\data\corpus' -Filter *.txt | Measure-Object -Property Length -Sum).Sum
Write-Output ("语料总字节: {0:N0} ({1:N2} MB)" -f $s, ($s/1MB))
```

**② 改词表大小：`config.json` → `train.bpe_vocab`**

语料变大后词表要跟着涨，否则平均每 token 承载的字节数太少（中文尤甚）：

| 语料规模 | 建议 `bpe_vocab` |
|---------|-----------------|
| 240 KB（当前） | 4096 |
| 2.4 MB | 8192 |
| 15 MB | 16384 |

约束：`bpe_vocab >= 256`（`src/config.rs:128`）。

> **⚠️ 实测：BPE 构建远比预期慢，且是每次启动的一次性成本。**
> `tokenizer.rs:126` 的实现是朴素 O(合并数 × 语料长度)：每次合并都要把**整个 ids 序列**做一遍
> HashMap 统计 + 一遍重写。
>
> | 语料 | 词表 | 合并次数 | 实测耗时 |
> |---|---|---|---|
> | 240 KB | 4096 | 3840 | ~30 秒 |
> | 4.15 MB | 8192 | 7936 | 23.7 分钟 |
> | **13.37 MB** | **8192** | **7936** | **~85 分钟** |
>
> **避免重复付费的办法**：首次训练完会把词表存到 `{out_dir}/tokenizer.json`，
> 之后把 `config.json` 的 `tokenizer_file` 指向它即可跳过重建：
>
> ```jsonc
> "tokenizer_file": "checkpoints/tokenizer.json"   // null → 该路径
> ```
>
> 续训（`--resume`）时尤其必要——否则每续一次就白等 20 多分钟。

**③ 改训练步数：`config.json` → `train.steps`**

语料变大后，每个 epoch 变长，需要更多步数才能充分学习：

| 语料 | 建议 `steps` | 建议 `warmup_steps` | 建议 `eval_every` |
|------|-------------|-------------------|------------------|
| 240 KB | 3000 | 200 | 100 |
| 2.4 MB | 8000 | 400 | 200 |
| 15 MB | 20000 | 600 | 500 |

约束：`warmup_steps <= steps`（`src/config.rs:118`）。

**④ 学习率可能需要微调**

`batch_size` 不变时语料变大，梯度噪声降低，`max_lr` 可以适当提高：

- 当前 `max_lr: 3e-4`（小语料下的保守值）
- 语料 ×10 后可试 `5e-4 ~ 6e-4`
- **不要直接跳到 1e-3**——已验证 1e-3 会发散（虽然那次也叠加了 flash attention 的 bug，但保守起见先小步加）

### 2.3 完整改动示例（语料 ×10 → 2.4 MB）

`config.json`：

```jsonc
{
  "model": {
    "vocab_size": 0,
    "n_embd": 256,
    "n_head": 8,
    "n_layer": 4,
    "block_size": 256,
    "n_kv_head": 0,
    "use_rmsnorm": false,
    "use_swiglu": false,
    "dropout": 0.0
  },
  "train": {
    "seed": 42,
    "batch_size": 16,
    "steps": 8000,          // 3000 → 8000
    "max_lr": 5e-4,         // 3e-4 → 5e-4
    "min_lr": 5e-5,         // 等比
    "warmup_steps": 400,    // 200 → 400
    "weight_decay": 0.01,
    "grad_clip": 1.0,
    "eval_every": 200,      // 100 → 200
    "eval_iters": 20,
    "tokenizer": "bpe",
    "bpe_vocab": 8192,      // 4096 → 8192
    "train_file": "data/corpus/",
    "val_file": null,
    "out_dir": "checkpoints",
    "accum_steps": 1,
    "tokenizer_file": null,
    "lora": null,
    "log_file": null,
    "early_stop_patience": 10
  }
}
```

### 2.4 执行

```powershell
# 1. 备份现有 checkpoint（新语料会重建 tokenizer，旧 ckpt 将无法解码）
New-Item -ItemType Directory -Path 'D:\Code\Rust\llm_from_scratch\ckpt_240kb' -Force | Out-Null
Move-Item 'D:\Code\Rust\llm_from_scratch\checkpoints\*' 'D:\Code\Rust\llm_from_scratch\ckpt_240kb\' -Force

# 2. 放入新语料（自行准备）
Copy-Item 'D:\我的文本\*.txt' 'D:\Code\Rust\llm_from_scratch\data\corpus\'

# 3. 改 config.json（见 2.3）

# 4. 启动
$c = 'cmd.exe /c cd /d "D:\Code\Rust\llm_from_scratch" && target\release\llm_from_scratch.exe train --config config.json > logs\train_v5.log 2>&1'
Invoke-CimMethod -ClassName Win32_Process -MethodName Create -Arguments @{CommandLine=$c}
```

> **⚠️ 词表一变（如 4096→8192），旧 checkpoint 全部作废**（`tokenizer.json` 会被覆盖重建）。
> 所以务必先 `Move-Item` 备份，别直接覆盖。

### 2.5 预期

- 参数/token 从 69 降到 6.9 → 过拟合大幅缓解
- val bpb 有望从 2.884 降到 **2.2 ~ 2.5**
- 训练时间：8000 步 × ~16s ≈ 36 小时（语料变大还会更慢），**建议先跑 1000 步看趋势**

---

## 3. 方向 B：缩小模型（治标，可与 A 组合）

### 3.1 原理

参数少 → 记忆能力弱 → 更早停止过拟合、更多依赖泛化。但**表达能力也下降**，val bpb 的下限会抬高。语料不增加时，这个方向的净收益有限。

### 3.2 改哪里

**只改 `config.json` 的 `model` 段**，不用动任何代码：

| 字段 | 当前位置 | 说明 |
|------|---------|------|
| `n_embd` | `config.json:4` | 隐藏维度，**必须能被 `n_head` 整除** |
| `n_head` | `config.json:5` | 注意力头数 |
| `n_layer` | `config.json:6` | 层数 |
| `block_size` | `config.json:7` | 上下文长度，**不要随意改小**（会改变任务难度，破坏与历史结果的可比性） |

约束（`src/config.rs:130-138`）：

```
n_embd % n_head == 0          ← 否则 head_dim 不是整数，直接 panic
n_layer >= 1
n_embd >= 1
```

### 3.3 候选配置（按参数量估算公式算）

| 方案 | n_embd | n_head | n_layer | 估算参数 | 参数/token |
|------|--------|--------|---------|---------|-----------|
| 当前 | 256 | 8 | 4 | 4,208,128 | 69 |
| **B1（激进）** | 128 | 4 | 2 | ~921,000 | 15 |
| **B2（折中）** | 128 | 4 | 4 | ~1,318,000 | 22 |
| B3（温和） | 192 | 6 | 3 | ~2,121,000 | 35 |
| B4（仅减层） | 256 | 8 | 2 | ~2,633,000 | 43 |

> 注意 `vocab × d` 这一项占比很大：vocab=4096、d=128 时嵌入表就占 524,288（B1 的 57%）。
> 所以缩小模型时词表不宜同时过大，否则省下来的参数又被嵌入表吃掉。

B1 的逐项计算（供核对）：
```
4096×128                      = 524,288     嵌入表
每层 12×128² + 13×128          = 198,272
2 层                          = 396,544
ln_f 2×128                    = 256
合计                          = 921,088
```

### 3.4 执行（以 B1 为例）

`config.json` 只改三行：

```jsonc
"n_embd": 128,     // 256 → 128
"n_head": 4,       // 8 → 4
"n_layer": 2,      // 4 → 2
```

并同步降低训练量（小模型收敛更快，步数可减少）：

```jsonc
"steps": 2000,         // 3000 → 2000
"eval_every": 100,
```

```powershell
# 备份（模型结构变了，旧 ckpt 无法加载）
New-Item -ItemType Directory -Path 'D:\Code\Rust\llm_from_scratch\ckpt_4M' -Force | Out-Null
Move-Item 'D:\Code\Rust\llm_from_scratch\checkpoints\*' 'D:\Code\Rust\llm_from_scratch\ckpt_4M\' -Force

# 启动
$c = 'cmd.exe /c cd /d "D:\Code\Rust\llm_from_scratch" && target\release\llm_from_scratch.exe train --config config.json > logs\train_v5.log 2>&1'
Invoke-CimMethod -ClassName Win32_Process -MethodName Create -Arguments @{CommandLine=$c}
```

> **⚠️ 模型结构（n_embd/n_head/n_layer/block_size/vocab_size）一变，旧 checkpoint 全部作废。**
> 启动横幅会打印新的参数总量，核对一下是否符合预期。

### 3.5 预期

- 语料不变时，val bpb 可能略好（2.8 → 2.7 左右）或**反而更差**（容量不足）
- 训练速度提升（B1 约快 3~4 倍）
- **单独用 B 天花板很低，建议只作为 A 的补充**

---

## 4. 方向 C：加正则化（治标，改动最小）

### 4.1 改哪里

**① `config.json` → `model.dropout`**

当前 `0.0`。这是我们之前刻意关掉的（当时误判为欠拟合，实为 flash attention bug）。

- 位置：`config.json:11`
- 取值范围：`[0, 1)`（`src/config.rs:135`）

**代码中的实际施放点**（无需修改，仅供理解 `dropout` 影响了哪些位置）：

| 文件:行 | 位置 |
|---------|------|
| `src/model.rs:206` | token embedding 输出后 |
| `src/model.rs:130` | 每个 block 的**注意力输出**后（残差相加前） |
| `src/model.rs:135` | 每个 block 的 **MLP 输出**后（残差相加前） |

4 层模型 = 1 + 4×2 = **9 处 dropout**。

**② `config.json` → `train.weight_decay`**

当前 `0.01`。AdamW 解耦权重衰减，加大可抑制权重增长。

- 位置：`config.json:20`
- 约束：`>= 0`（`src/config.rs:125`）
- 实现：`src/optim.rs:151-152`，`d[j] = d[j] - step - lr*wd*d[j]`

### 4.2 建议取值

| 参数 | 当前 | 建议 | 说明 |
|------|------|------|------|
| `model.dropout` | 0.0 | **0.1**（激进可 0.2） | 太大（>0.3）会明显拖慢收敛 |
| `train.weight_decay` | 0.01 | **0.1** | 10 倍；再大可能压死学习 |

### 4.3 完整改动

```jsonc
{
  "model": {
    "...": "...",
    "dropout": 0.1          // 0.0 → 0.1
  },
  "train": {
    "...": "...",
    "weight_decay": 0.1     // 0.01 → 0.1
  }
}
```

### 4.4 执行

```powershell
# dropout 和 weight_decay 都不改变模型结构，旧 ckpt 仍可加载/续训
# 但为了干净对比，建议新开 out_dir
```

修改 `config.json:28`：

```jsonc
"out_dir": "checkpoints_reg",    // "checkpoints" → "checkpoints_reg"
```

```powershell
$c = 'cmd.exe /c cd /d "D:\Code\Rust\llm_from_scratch" && target\release\llm_from_scratch.exe train --config config.json > logs\train_v5.log 2>&1'
Invoke-CimMethod -ClassName Win32_Process -MethodName Create -Arguments @{CommandLine=$c}
```

> 注意：`out_dir` 改了以后，`generate`/`eval` 需要显式指定分词器路径（它默认从 `{out_dir}/tokenizer.json` 找）：
> ```powershell
> target\release\llm_from_scratch.exe generate --ckpt checkpoints_reg/best.ckpt --tokenizer checkpoints_reg/tokenizer.json --prompt '人工智能'
> ```
> 或者用旧路径的 tokenizer（`dropout`/`weight_decay` 不改词表，两个 tokenizer 是同一个）：
> ```powershell
> target\release\llm_from_scratch.exe generate --ckpt checkpoints_reg/best.ckpt --tokenizer ckpt_240kb/tokenizer.json --prompt '人工智能'
> ```

### 4.5 预期

- 训练 loss 下降变慢（dropout 引入噪声，这是正常的）
- val bpb 改善**有限**（2.884 → 2.8 左右）
- **注意**：`dropout > 0` 时会走 `tensor.rs` 的 dropout 分支。该分支的种子 bug 已修复
  （详见附录），现在每次调用都会推进随机流，可以放心用

---

## 5. 三个方向对比与组合建议

| 方向 | 治本？ | 改动量 | 预期 val bpb | 训练耗时 | 风险 |
|------|-------|--------|-------------|---------|------|
| **A 扩语料** | ✅ 治本 | 中（备数据 + 调 4 个参数） | **2.2 ~ 2.5** | 36h+ | 低（数据质量决定上限） |
| **B 缩模型** | ❌ 治标 | 小（改 3 行） | 2.7 ~ 2.9（可能更差） | 明显更快 | 容量不足 |
| **C 加正则** | ❌ 治标 | 最小（改 2 个值） | 2.8 左右 | 不变 | 太低无效、太高拖慢 |

### 推荐组合

**优先级 1（单做）**：**A**。语料不解决，B 和 C 加起来也到不了 2.0。

**优先级 2（A + C）**：扩语料的同时把 `dropout: 0.1`、`weight_decay: 0.1` 打开。
语料仍偏小时，正则化能挤压出额外收益。

**优先级 3（A + B + C）**：如果新语料只有 1~2 MB（×5~8），建议同时把模型缩到 B2（~1.3M 参数），
让参数/token 逼近 1，三者协同效果最好。

### 不建议

- **单独做 B 或 C**：天花板太低，跑 10 小时可能只换来 0.05 bpb。
- **继续调学习率**：已经验证 lr 不是瓶颈（bug 修复后 3e-4 稳定收敛）。

---

## 6. 验证判据（怎么判断改对了）

跑起来后，按顺序检查这四项：

**① 梯度是否正常**（最重要）

```powershell
Get-Content logs\train_v5.log -Encoding UTF8 | Select-String '/\d+ \| loss' | Select-Object -Last 5
```

| 现象 | 含义 |
|------|------|
| `grad` 在 **0.1 ~ 5** 之间波动，末尾 `*` 时有时无 | ✅ 正常 |
| `grad` 单调上升，几百步后破百、破千 | ❌ 仍有反向 bug 或 lr 过大 |
| `grad` 出现 `NaN` / `inf` | ❌ 数值爆炸 |

**② 是否还在过拟合**

看 train bpb 和 val bpb 的**差距变化**：

| 现象 | 含义 |
|------|------|
| 两者同步下降，差距 < 0.5 bpb | ✅ 泛化良好 |
| train bpb 一路降到 < 0.5，val bpb 反弹 | ❌ 仍在过拟合 → 回到方向 A |

**③ val bpb 是否突破**

```powershell
# 用 1.5 节的监控命令；重点看什么时候出现最低点
```

目标：**< 2.6**（A 方向）/ **< 2.9**（B、C 方向）。

**④ 生成是否改善**

```powershell
target\release\llm_from_scratch.exe generate --ckpt checkpoints/best.ckpt --prompt '人工智能' --max-new 60 --temperature 0.5
```

| 现象 | 含义 |
|------|------|
| 出现 `正在正在正在正在` 这类连续重复 | 模型太小 / 欠训练，典型重复退化 |
| 出现 `�` 替换字符 | 生成了非法 UTF-8 字节序列，说明模型对字节级结构掌握不足 |
| 输出通顺、无重复 | ✅ 成功 |

---

## 7. 实测结果（2026-09-15）

### 7.1 实验设置

- 同机 CPU（8 逻辑核）、release 构建、固定 `seed=42`、语料不变（90 文件 / 240,251 字节 / 67,296 token）
- 每组独立 `out_dir`，避免互相覆盖；统一按 `bpb = val_loss × 0.40412` 换算
- 四组配置：`config_probe_base.json` / `config_probe_reg.json`（C）/ `config_probe_b2.json`（B2）/ `config_probe_b2c.json`（B2+C）
- 对应日志：`logs\probe_base.log` / `logs\probe_reg.log` / `logs\probe_b2.log` / `logs\probe_b2c.log`
- 步数：C 组按约定停在 step 500（峰值未到）；B2 组采到 900；B2+C 组跑到早停（step 1900）
- 基线（4.21M 模型）直接复用历史 `logs\train_v4.log`——已先用同机 CPU 复跑到 step 100，数值**逐位吻合**（train 7.5915 / val 7.7565 / bpb 3.135）

### 7.2 val bpb 全表

| step | 基线 4.21M | C 4.21M+正则 | B2 1.32M | **B2+C 1.32M+正则** |
|------|-----------|-------------|----------|-------------------|
| 100 | 3.135 | 3.141 | 3.268 | 3.276 |
| 200 | 2.975 | 2.980 | 3.012 | 3.015 |
| 300 | 2.901 | 2.919 | 2.939 | 2.941 |
| 400 | 2.891 | 2.876 | 2.936 | 2.941 |
| 500 | **2.884** | **2.832** | 2.895 | 2.894 |
| 600 | 2.951 | — | 2.867 | 2.862 |
| 700 | — | — | **2.867** | 2.837 |
| 800 | 3.106 | — | 2.897 | 2.808 |
| 900 | — | — | 2.930 | **2.794** ← 最低点 |
| 1000 | 3.231 | — | — | 2.801 |
| 1100 | — | — | — | 2.806 |
| 1200 | — | — | — | 2.827 |
| 1300 | — | — | — | 2.852 |
| 1400 | — | — | — | 2.873 |
| 1500 | 3.455 | — | — | 2.879 |
| 1600 | — | — | — | 2.908 |
| 1700 | — | — | — | 2.914 |
| 1800 | — | — | — | 2.931 |
| 1900 | — | — | — | 2.952 |

### 7.3 各组最低点与效率

| 配置 | 最低 val bpb | 出现步数 | 参数量 | ckpt | 每步耗时 |
|------|-------------|---------|--------|------|---------|
| 基线 4.21M | 2.884 | 500 | 4,208,128 | 134.8 MB | ~16 s（历史 GPU） |
| C 4.21M+正则 | 2.832 | 500（未到峰值） | 4,208,128 | 134.8 MB | ~21.6 s |
| B2 1.32M | 2.867 | 600~700 | 1,317,632 | 41.8 MB | ~9.3 s |
| **B2+C 1.32M+正则** | **2.794** | **900** | 1,317,632 | 41.9 MB | **8.01 s** |

### 7.4 结论

**① B2+C 组合最优，且有真实协同**

`B2+C 2.794` ＜ `C 2.832` ＜ `B2 2.867` ＜ `基线 2.884`。

C 单独 2.832、B2 单独 2.867，组合后 2.794 —— **优于两者任一单独结果**。机制互补：B 砍掉记忆容量，C 抑制权重增长，共同堵死"背训练集"这条路。

**② 曲线形状的改善比最低点更有价值**

- 基线：500 步见顶后崩盘（800→3.106，1500→3.455）
- B2+C：700~1300 步是 **2.79~2.85 的宽平台**，最优推迟到 900 步，之后仅缓慢退化

按第 6 节判据 ②，B2+C 在最优点 train bpb `2.277` / val bpb `2.794`，**差距 0.517**，落在"泛化良好（< 0.5）"边界；基线在 step 500 时 train `1.872` / val `2.884`，**差距 1.012**。过拟合被实质压制。

**③ 效率大幅改善**

参数量 −69%、ckpt −69%、训练速度 ≈2×（对比同机 CPU 的 21.6 s/步为 2.7×），同时 val bpb 还低了 0.090（−3.1%）。

**④ 达标情况**

- 第 6 节判据 ③ 对 B/C 类方向要求 **< 2.9** → B2+C 的 2.794 **达标** ✅
- A 方向目标 **< 2.6** → **未达** ❌
- 生成通顺所需的 **< 2.0** → **远未达** ❌

**⑤ 生成质量仍然不合格（判据 ④）**

```text
B2+C：人工智能的性的的在性的式。 / 强的的强的的的和和。 / 强在在在在。
基线：人工智能在成的在在个来在AI在了来的加被的技术。 ... 现�数据是我在代当个在得？
```

两组都是典型**重复退化**（`的的`、`在在在在`），基线还生成了 `�`（非法 UTF-8 字节）。说明 0.09 bpb 的改善**没有跨过"通顺"的门槛**——与 bpb 仍高达 2.79（目标 < 2.0）一致。

### 7.5 对本文档原有预期的修正

| 原预期 | 实测 | 是否吻合 |
|--------|------|---------|
| 4.5 节：C → 2.8 左右 | 2.832（且峰值未到） | ✅ 吻合 |
| 3.5 节：B → 2.7~2.9 | 2.867 | ✅ 吻合 |
| 5 节：单独做 B 或 C 只换来 0.05 bpb | C 换来 0.052、B 换来 0.017 | ✅ 吻合 |
| 3.1 节：B 参数少 → 更早停止过拟合 | 最优点从 500 **推迟到 600~700** | ❌ **相反** |
| — | **B 与 C 有协同，组合优于任一单独** | 🆕 新发现 |

> **修正 1**：B 缩小模型**并不会**让过拟合更早停止，反而是**更晚**（记忆容量小 → 需要更多步才背完训练集）。所以小模型的训练步数不该像 3.4 节建议的那样"同步降低"，反而要适当拉长。
>
> **修正 2**：B 和 C 不是二选一，**组合使用收益明显更大**（0.052 / 0.017 → 0.090）。

### 7.6 保留意见（结论强度的诚实说明）

1. **差异量级接近评估噪声**：0.017~0.090 bpb 与 `eval_iters=20` 的评估噪声同量级。其中 B2 相对基线仅 0.017，只能算**弱证据**；C 的 0.052 和 B2+C 的 0.090 相对可信，且有明确机制佐证（train/val 差距从 1.012 缩到 0.517）。
2. **C 组比较不公平**：C 在 step 500 被停时曲线**仍在下降、峰值未到**。C 若跑到 900 步，可能也会降到 2.79 附近。C 的真实下限仍未知。
3. **机器负载有波动**：实验期间 CPU 负载 96%（Trae/Lark 等争抢），tok/s 在 182~488 之间波动，但不影响 loss/bpb 数值（固定 seed，数值可复现）。

### 7.7 小结（B / C 方向）

方向 B、C 都已验证完毕，两者合计只换来 **0.090 bpb**，与第 5 节"天花板很低"的判断一致。

**要突破 2.6 / 2.0，只剩方向 A（扩语料）这一条路**——240 KB / 67K token 对 1.32M 参数仍是 22 倍。
实际执行时按"三者同开"处理（2 节 + 3 节 + 4 节），结果见 7.8。

### 7.8 方向 A 实测（扩语料，2026-09-15）

#### 7.8.1 实验设置

| 项 | 值 |
|---|---|
| 语料 | `data/corpus/` **93 文件 / 4,152,560 字节（3.96 MB）** |
| 新增文件 | `zh_classic_sanguoyanyi.txt`(1.86 MB) / `en_classic_moby_dick.txt`(1.28 MB) / `en_classic_pride_prejudice.txt`(0.77 MB) |
| 词表 | BPE **8192**（构建耗时 23.7 分钟，见 2.2；已改用 `tokenizer_file` 跳过重建） |
| 模型 | `n_embd=128` / `n_head=4` / `n_layer=4` → **1,841,920 参数** |
| 正则 | `dropout=0.1` + `weight_decay=0.1`（即 **A+B2+C 三者同开**） |
| token | 1,058,589（训练 952,730 / 验证 105,859） |
| 参数/token | **1.93**（旧 4.21M 模型换到同一语料下是 4.72） |
| 步数 | 8000 / `eval_every=200`；日志 `logs\train_a.log`（step 800 前）+ `logs\train_a2.log`（续训） |
| bpb 系数 | **0.3678**（语料变了，见 1.6） |

> 训练中途进程被终端连带杀掉一次（step 878，详见 1.3），已从 `latest.ckpt`(step 800) `--resume` 无缝续跑，
> 续训后 loss 与中断点连续，两组数据可拼接使用。

#### 7.8.2 val bpb 曲线

| step | train | val | bpb | gap |
|---|---|---|---|---|
| 200 | 8.0063 | 8.1532 | 2.999 | 0.147 |
| 400 | 7.6829 | 7.8281 | 2.879 | 0.145 |
| 600 | 7.2414 | 7.5498 | 2.777 | 0.308 |
| 800 | 7.0860 | 7.3881 | 2.717 | 0.302 |
| 1000 | 6.8608 | 7.2130 | 2.653 | 0.352 |
| 1200 | 6.6243 | 7.0082 | 2.578 | 0.384 |
| 1400 | 6.4363 | 6.8098 | 2.505 | 0.374 |
| 1600 | 6.1471 | 6.7046 | 2.466 | 0.558 |
| 1800 | 5.9208 | 6.5807 | 2.420 | 0.660 |
| 2000 | 5.9120 | 6.4973 | 2.390 | 0.585 |
| 2200 | 5.8181 | 6.5045 | 2.392 | 0.686 |
| **2400** | 5.6560 | 6.4212 | **2.362** ← 最低 | 0.765 |
| 2600 | 5.6611 | 6.4246 | 2.363 | 0.764 |

#### 7.8.3 与历史最优对比

| 配置 | 最低 val bpb | 出现步数 | 参数量 |
|---|---|---|---|
| 基线（240 KB / 4.21M） | 2.884 | 500 | 4,208,128 |
| B2+C（240 KB / 1.32M） | 2.794 | 900 | 1,317,632 |
| **A+B2+C（4.15 MB / 1.84M）** | **2.362** | 2400 | 1,841,920 |

- 相对旧最优 **−0.432 bpb（−15.5%）**；相对原始基线 **−0.522（−18.1%）**
- **判据 ① 梯度**：全程 0.50~0.60 稳定，无异常 ✅
- **判据 ③ 达标**：A 方向目标 `< 2.6` → **step 1200 即达标（2.578）** ✅

> 幅度对比：单纯 B/C 合计只有 0.090，A 一个方向就是它的 **4.8 倍**——第 5 节"A 才治本"的判断成立。

#### 7.8.4 判据 ②：仍在过拟合（未解决）

gap（val bpb − train bpb）**从 0.147 单调扩大到 0.765**，step 1600 起稳定越过 0.5 阈值：

| step | 1200 | 1400 | 1600 | 1800 | 2000 | 2400 |
|---|---|---|---|---|---|---|
| gap | 0.384 | 0.374 | **0.558** | 0.660 | 0.585 | **0.765** |

- 对比 B2+C 在最优点的 gap 是 0.517，**扩语料后反而更大**
- 说明参数/token 降到 1.93 并**没有**消除过拟合（见修正 3）

#### 7.8.5 判据 ④：生成质量（重复退化已消除，但未通顺）

用 `best.ckpt`（step 2400 快照，`--temperature 0.8 --top-k 40`）：

**① `人工智能`（中文）**

```text
人工智能。能一年人。能生
有大化。一家在家人人；有天的行
是人。它有更所。冃大法训在极庥。数据、机作AI的数据新动化作训。对是是AI新的新维问题。
```

→ 句子不成句，但**出现了连贯的专业词**（`数据`、`AI`、`训`、`系统`），
且**完全没有**旧基线那种 `的的的的` / `在在在在` 式连续重复，也没有 `�` 非法字节。

**② `The meaning of life is`（英文）**

```text
The meaning of life is, that he made no great fitate of the first one whale, it was some to a
... to find the Sperm Whale fishery, ... But the Right Whale, and his first final of which the whale
was all of whales in the whale, as the whale of the Sperm Whale's whales ...
```

→ **语法正确、长程结构完整的大段英文**。但这是**语料记忆**（Moby Dick 片段逐字复现），不是泛化。

**③ `有一天`（中文）**

```text
有一天。若在手手一箭，正死者被！」言訖，操謂操曰：「…吾之故之所…慈曰：「此今已在死也！」
眾皆喜曰：「吾若聞我去，汝可以說此意？」遂引行。平曰：「是吾主人也！」
```

→ 完整的《三国演义》文言句式与对白格式，同样是**记忆**。

**小结**：判据 ④ 的三个负面现象（连续重复 / `�` / 完全不成词）都已消失，
模型已能复现长程语言结构；但仍**无法脱离记忆做创新组合**，未跨过"通顺"门槛——与 bpb 2.362 > 2.0 一致。

#### 7.8.6 效率

- **9.3 s/步**（续训 1802 步 / 16790 s），≈470 tok/s（同机 CPU，8 逻辑核）
- 全量 8000 步预计 ~21 小时（旧基线在 240 KB 上是 16 s/步，虽然语料大了 17 倍但模型小了 2.3 倍）
- 8192 词表的 BPE 重建成本 23.7 分钟已用 `tokenizer_file` 规避

#### 7.8.7 对本文档原有预期的修正（续 7.5）

| 原预期 | 实测 | 是否吻合 |
|--------|------|---------|
| 2.5 节：A → val bpb **2.2 ~ 2.5** | **2.362** | ✅ 落在区间低端 |
| 2.1 节：参数/token 降到 ~1 即"基本不过拟合" | 参数/token **1.93** 时 gap 仍达 **0.765** | ❌ **不吻合** |
| 5 节：A 治本，B/C 治标 | A −0.432 vs B/C 合计 −0.090 | ✅ 吻合 |
| 2.5 节：8000 步 ×16 s ≈ 36 小时 | 9.3 s/步 → ~21 小时 | ✅ 更快 |

> **修正 3**：**"参数量 ≈ token 数即不过拟合"这条经验规则在本项目不成立。**
> 参数/token 从 22 降到 1.93（−91%），gap 却从 0.517 涨到 0.765。
> 最可能的原因：语料**异质性太大**（3 本书、中英混合、文言与现代文混杂），
> 模型优先记住训练集里的**具体片段**（英文段落几乎逐字复现）而不是通用结构。
> 推论：要压到 2.0 以下，关键不是继续降参数/token，而是**更多、更同质的语料**
> （例如同一题材/同一语言扩大到 15 MB+，而不是继续拼凑不同来源）。

> **修正 4**：`block_size=256` 的上下文对这种大语料明显偏小（英文整段记忆超过 256 token 就会截断），
> 若继续做 A 方向，应把 `block_size` 一并提到 512，并相应降低 `batch_size`。

---

## 8. 方向 A2：中文古典同质语料 + block_size 512（进行中）

> 启动时间：2026-09-15 晚。动机：7.8 证明"扩语料"最有效，但也暴露两个问题 ——
> ① 语料**异质性**（中英混合 + 文言/现代/代码/对话混杂）导致参数/token 降到 1.93 仍在过拟合（修正 3）；
> ② `block_size=256` 太短（修正 4）。
> A2 针对这两点重做，同时**只改语料与尺寸，不改代码**。

### 8.1 语料：换成单一语言 + 同一文体的明清白话长篇

旧 `data/corpus/` 保留不动，新语料放在独立目录 `data/corpus_zh/`，通过 `train_file` 指向：

| 文件 | Gutenberg ID | 字节 |
|---|---|---|
| `zh_classic_jinguqiguan.txt` 今古奇觀 | 24230 | 2,896,630 |
| `zh_classic_hongloumeng.txt` 紅樓夢 | 24264 | 2,663,455 |
| `zh_classic_xiyouji.txt` 西遊記 | 23962 | 2,264,069 |
| `zh_classic_suitangyanyi.txt` 隋唐演義 | 23835 | 1,865,408 |
| `zh_classic_sanguoyanyi.txt` 三國志演義 | 23950 | 1,863,656 |
| `zh_classic_fengshenyanyi.txt` 封神演義 | 23910 | 1,820,341 |
| **合计** | | **13,373,559（12.75 MB）** |

下载命令（可复用；Gutendex 返回的 JSON 在 PS 5.1 下会按 Latin-1 误解码，必须手动按 UTF-8 解字节）：

```powershell
$dst = 'D:\Code\Rust\llm_from_scratch\data\corpus_zh'
New-Item -ItemType Directory -Path $dst -Force | Out-Null
@(24230,24264,23962,23835,23950,23910) | ForEach-Object {
  Invoke-WebRequest -Uri "https://www.gutenberg.org/cache/epub/$_/pg$_.txt" `
    -OutFile (Join-Path $dst "$_.txt") -UseBasicParsing
  Start-Sleep -Seconds 2
}
```

```powershell
# 查 Gutenberg 中文书目（标题要手动解 UTF-8，否则显示乱码）
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$resp = Invoke-WebRequest -Uri 'https://gutendex.com/books?languages=zh' -UseBasicParsing
$r = [System.Text.Encoding]::UTF8.GetString($resp.RawContentStream.ToArray()) | ConvertFrom-Json
$r.results | ForEach-Object { "{0,6}  {1,6}  {2}" -f $_.id, $_.download_count, $_.title }
```

### 8.2 配置改动（`config.json`）

| 字段 | A（4.15MB 混合） | **A2（12.75MB 中文同质）** | 理由 |
|---|---|---|---|
| `model.block_size` | 256 | **512** | 修正 4 |
| `train.batch_size` | 16 | **8** | block 翻倍后保持每步 token 数不变（8×512 = 16×256 = 4096） |
| `train.train_file` | `data/corpus/` | **`data/corpus_zh/`** | 换语料 |
| `train.steps` | 8000 | **4000** | 每步 token 数相同，但语料大 3 倍 → 4000 步 ≈ 3.9 个 epoch |
| `train.warmup_steps` | 400 | **300** | 总步数减半 |
| `train.out_dir` | `checkpoints` | **`checkpoints_zh`** | 隔离产物 |
| `train.tokenizer_file` | `checkpoints/tokenizer.json` | **`null`** | 语料变了，必须重建 |
| `train.bpe_vocab` | 8192 | 8192 | 不变（不优化 BPE 的前提下，控制一次性成本） |
| `model.dropout` / `weight_decay` | 0.1 / 0.1 | 0.1 / 0.1 | 保持 C 方向 |
| `model.n_embd`/`n_head`/`n_layer` | 128/4/4 | 128/4/4 | 保持 B2 |

> **bpb 系数要重算**：语料换了，`0.3678` 失效。启动横幅会打印新的 token 数，
> 按 `(n_tokens / 13373559) / ln(2)` 重算（见 1.6）。

### 8.3 执行

```powershell
$base = 'D:\Code\Rust\llm_from_scratch'
$exe  = "$base\target\release\llm_from_scratch.exe"
$inner = "Start-Process -FilePath '" + $exe + "' -ArgumentList 'train --config config.json' " +
         "-RedirectStandardOutput '" + $base + "\logs\train_zh.log' " +
         "-RedirectStandardError '" + $base + "\logs\train_zh.err.log' " +
         "-WorkingDirectory '" + $base + "' -NoNewWindow -Wait"
Invoke-CimMethod -ClassName Win32_Process -MethodName Create `
  -Arguments @{CommandLine=('powershell.exe -NoProfile -Command "' + $inner + '"')}
```

### 8.4 启动实测（已确认）

| 项 | 实测值 |
|---|---|
| BPE 重建耗时 | **~85 分钟**（13.37 MB / 7936 次合并；比 4.15 MB 的 23.7 分钟多 3.6 倍） |
| 词表 | 8192，已存到 `checkpoints_zh/tokenizer.json`（续训时把 `tokenizer_file` 指过去即可跳过重建） |
| token | **3,304,290**（训练 2,973,861 / 验证 330,429） |
| token/字节 | 0.2471 |
| 参数/token | **0.619**（A 是 1.93，原始基线是 22） |
| **bpb 系数** | **0.35646**（`(3304290/13373559)/ln2`，已补进 1.6 节） |
| 每步耗时 | **~15.4 s**（231 s / 15 步）→ 4000 步约 **17 小时** |
| 启动日志 | `logs\train_zh.log`（`logs\train_zh.err.log` 为空） |

```text
从目录 data/corpus_zh/ 加载了 6 个文本文件
分词器已保存到 checkpoints_zh/tokenizer.json
开始训练：bpe（vocab=8192）模型参数 1841920 | 语料 3304290 tokens（训练 2973861 / 验证 330429）| batch=8 block=512
[info] 早停已启用：patience=10（连续 10 次评估不改善则停止）
[train] step 1/4000 | loss 9.0305 | grad 0.47 | lr 0.000002 | 0.1 st/s | 244 tok/s | 17s | ~67127s
```

监控命令（系数换成 **0.35646**）：

```powershell
Get-Content 'd:\Code\Rust\llm_from_scratch\logs\train_zh.log' -Encoding UTF8 | ForEach-Object {
  if ($_ -match '^\s*step\s+(\d+) \| lr ([\d.]+) \| loss ([\d.]+) \| val ([\d.]+) \(ppl ([\d.]+)\)') {
    $tr=[double]$Matches[3]; $va=[double]$Matches[4]
    Write-Output ("step {0,5} | train {1} | val {2} | bpb {3:N3} | gap {4:N3}" -f `
      $Matches[1],$Matches[3],$Matches[4],($va*0.35646),($va-$tr))
  }
}
```

### 8.5 实测（阶段性，2026-09-16 18:35 中断）

训练跑到 **step 2845 / 4000** 时被**外部终止**：`logs\train_zh.log` 最后写入 18:35:35，
`logs\train_zh.err.log` 为空，Windows 事件日志无任何 Error/Critical 记录，也无 `[done]` 行 →
不是代码崩溃/OOM，而是被外部 `TerminateProcess`（与 1.3 节那个"终端回收连带杀进程"同一种签名）。
已跑到 10.5 小时，产物完整保留在 `checkpoints_zh/`。

| step | train | val | bpb | gap |
|---|---|---|---|---|
| 200 | 8.0864 | 8.2024 | 2.924 | 0.116 |
| 400 | 7.8807 | 8.0302 | 2.862 | 0.150 |
| 600 | 7.5890 | 7.8604 | 2.802 | 0.271 |
| 800 | 7.6193 | 7.7097 | 2.748 | 0.090 |
| 1000 | 7.3120 | 7.5194 | 2.680 | 0.207 |
| 1200 | 7.1705 | 7.3446 | 2.618 | 0.174 |
| 1400 | 6.9807 | 7.1944 | 2.565 | 0.214 |
| 1600 | 6.8216 | 7.1405 | 2.545 | 0.319 |
| 1800 | 6.8529 | 7.0302 | 2.506 | 0.177 |
| 2000 | 6.7226 | 6.9875 | 2.491 | 0.265 |
| 2200 | 6.7825 | 6.9258 | 2.469 | 0.143 |
| 2400 | 6.6619 | 6.8240 | 2.432 | 0.162 |
| **2600** | 6.7615 | 6.7011 | **2.389** ← 最低 | -0.060 |
| 2800 | 6.4785 | 6.7717 | 2.414 | 0.293 |

**关键观察：gap 大幅收敛。** A2 全程 gap 在 **-0.06 ~ 0.32**，而方向 A 在 step 2400 已是 **0.765**。
说明修正 3 的推论成立：**过拟合的元凶是语料异质性，不是参数量**——
换成单一语言、同一文体的语料后，参数/token 只从 1.93 降到 0.619，gap 却降了一个数量级。

> **注意**：这里 bpb 换了语料与 `block_size`，**与 7.x 的 2.362 不可直接比较**，只能看趋势
> （仍在下降，未见拐点：2600 的 2.389 比 2800 的 2.414 更低，但只差一个采样点）。
> 曲线在 2600 处 gap 为负（val 好于 train），属 dropout 噪声，不代表泛化优于训练。

### 8.6 训练可用性冒烟测试

`config_smoke.json`：`n_layer=2`、`block_size=128`、`batch_size=4`、`steps=20`、`eval_every=10`、
`out_dir=checkpoints_smoke`、`tokenizer_file=checkpoints_zh/tokenizer.json`（跳过 85 分钟重建）。

```powershell
target\release\llm_from_scratch.exe train --config config_smoke.json
```

实测 **14 秒跑完 20 步（0.69 s/步）**，全流程正常：

```text
已从 checkpoints_zh/tokenizer.json 加载分词器（bpe，词表 8192）
开始训练：bpe（vocab=8192）模型参数 1445376 | 语料 3304290 tokens（训练 2973861 / 验证 330429）| batch=4 block=128
step    10 | lr 0.000388 | loss 8.9664 | val 8.9473 (ppl 7687.3) | 730 tok/s *
step    20 | lr 0.000050 | loss 8.8503 | val 8.8643 (ppl 7074.6) | 744 tok/s *
[done] checkpoint 已保存到 checkpoints_smoke/ | 总耗时 14s | 0.69s/步（共 20 步）
```

验证点：语料加载 ✓ 分词器复用（免重建）✓ 前向/反向（grad 0.92~0.97）✓ 学习（train/val 同步下降）✓
LR warmup+cosine（5e-4 → 5e-5）✓ 评估 ✓ checkpoint 落盘 ✓ 无 NaN / 无 `�` ✓

> 这是个**可复用的健康检查**：以后改完代码或配置，先跑它（~15 秒）再启动长训练。

### 8.7 续训（2026-09-16）

被外部终止后，把 `config.json` 的 `tokenizer_file` 指到已存词表（省掉 85 分钟重建），再 `--resume`：

```jsonc
"tokenizer_file": "checkpoints_zh/tokenizer.json"   // null → 该路径
```

```powershell
$base = 'D:\Code\Rust\llm_from_scratch'
$exe  = "$base\target\release\llm_from_scratch.exe"
$inner = "Start-Process -FilePath '" + $exe + "' -ArgumentList 'train --config config.json --resume checkpoints_zh/latest.ckpt' " +
         "-RedirectStandardOutput '" + $base + "\logs\train_zh2.log' " +
         "-RedirectStandardError '" + $base + "\logs\train_zh2.err.log' " +
         "-WorkingDirectory '" + $base + "' -NoNewWindow -Wait"
Invoke-CimMethod -ClassName Win32_Process -MethodName Create `
  -Arguments @{CommandLine=('powershell.exe -NoProfile -Command "' + $inner + '"')}
```

续训成功标志（`logs\train_zh2.log` 抬头）：

```text
已从 checkpoints_zh/tokenizer.json 加载分词器（bpe，词表 8192）
已从 checkpoints_zh/latest.ckpt 恢复：step=2800，best_val_loss=6.7011
开始训练：bpe（vocab=8192）模型参数 1841920 | 语料 3304290 tokens（训练 2973861 / 验证 330429）| batch=8 block=512
[train] step 2801/4000 | loss 6.6092 | grad 0.54 | lr 0.000157 | 0.1 st/s | 277 tok/s | 15s | ~17715s
```

- loss 从 6.61 起步，与中断点（step 2800 的 train 6.4785）连续 ✓
- lr 从 1.57e-4 接续余弦衰减 ✓
- 剩余 1200 步，约 **4.5 小时**
- 注意 `best_val_loss=6.7011` 是 step 2600 的最优点，早停计数也随之恢复

### 8.8 GPU 加速实测：只有 9% 提升（2026-09-16）

#### 做了什么

```powershell
# 1. 先停掉 CPU 构建的训练（停在 step 3231，checkpoint 在 step 3200）
Get-Process | Where-Object { $_.ProcessName -like '*llm_from*' } | Stop-Process -Force
# 2. 开 gpu feature 重编译（依赖已缓存，19 秒）
cargo build --release --features gpu
# 3. 续训（日志 logs\train_zh3.log）
#    train --config config.json --resume checkpoints_zh/latest.ckpt
```

GPU 识别正常，自动选中独显：

```text
[gpu] 适配器 0: Intel(R) UHD Graphics 620 (IntegratedGpu, Dx12)
[gpu] 适配器 1: NVIDIA GeForce MX150 (DiscreteGpu, Dx12)
[gpu] 适配器 2: Microsoft Basic Render Driver (Cpu, Dx12)
[gpu] 已选择适配器: NVIDIA GeForce MX150 (DiscreteGpu)
GPU: NVIDIA GeForce MX150（Dx12）
```

#### 结果：数值完全一致，但速度几乎没变

冒烟测试（`config_smoke.json`，20 步）**与 CPU 版逐位相同**：
step 10 `loss 8.9664 | val 8.9473`、step 20 `loss 8.8503 | val 8.8643`
→ 说明 GPU 路径**数值正确**，`[done] matmul 分流：GPU 320 / CPU 512`（仅 38% 的矩阵乘走 GPU）。

真正配置（batch 8 / block 512 / 4 层）的速度对比：

| 构建 | 每步耗时 | tok/s | 分流 |
|---|---|---|---|
| CPU（step 2801~3231） | ~15.4 s | 277~308 | 全 CPU |
| **GPU（step 3201 起）** | **~14.0 s** | 277~295 | GPU 大矩阵 + CPU 其余 |

**只有 ~9% 提升**，与"MX150 峰值算力是 CPU 的 20 倍"的预期相差极远。

#### 为什么只快 9%（三条原因，均可在代码里验证）

1. **每次 dispatch 固定开销 ~10ms**（`gpu.rs:20` 注释实测），且**每次调用都要把输入上传、输出回读到 CPU**（`queue.write_buffer` + 映射读回，PCIe 往返）。LM 输出头一次的输出就是 `[4096, 8192]` = **134 MB**，来回搬一遍比算还贵。
2. **只有 ≥ 5000 万 FLOPs 的矩阵乘才走 GPU**（`MATMUL_MIN_FLOPS = 50_000_000`，`gpu.rs:23`），注意力头内积等中等规模算子全部留在 CPU —— 而 4 层 × 前后向的这些小算子加起来仍是可观的一部分。
3. **MX150 是入门级低压独显**（~1.1 TFLOP 峰值 FP32），且着色器是 **16×16 分块的教学实现**（`gpu.rs:576` 注释写明），实际算力远达不到峰值。

> **结论**：GPU 后端**能用、数值正确**，但在"每算子独立上传/下载 + 大阈值"的架构下，
> 对这个小模型只有个位数百分比收益。**真正的瓶颈是手写 CPU matmul 的有效算力（~3.8 GFLOP/s）**，
> 而不是没上 GPU。想拿到数量级提升，需要的是"整个 step 常驻 GPU、消除每算子 PCIe 往返"
> 或"CPU matmul 分块 + 显式 SIMD"，两者都是代码改动。

#### 怎么确认"GPU 到底有没有在用"（可复用）

任务管理器里 GPU 一栏长期显示 0%，很容易误判成"根本没上 GPU"。用 `nvidia-smi` 查计算进程才是铁证：

```powershell
nvidia-smi --query-compute-apps=pid,process_name,used_memory --format=csv
nvidia-smi --query-gpu=name,utilization.gpu,memory.used,memory.total --format=csv
```

实测输出（训练进行到 step 3255 时）：

```text
pid, process_name, used_gpu_memory [MiB]
21752, D:\Code\Rust\llm_from_scratch\target\release\llm_from_scratch.exe, [N/A]

name, utilization.gpu [%], memory.used [MiB], memory.total [MiB]
NVIDIA GeForce MX150, 0 %, 427 MiB, 2048 MiB
```

**进程确实挂在 MX150 上、占了 427 MiB 显存 → GPU 是在用的**；
但 `utilization.gpu = 0%` → **GPU 99% 的时间在闲着**。
两者不矛盾：只有 ≥50 MFLOP 的矩阵乘才上 GPU，每次调用还要等 PCIe 搬运，
GPU 干完 10ms 就得等 CPU 把数据搬回来；而 dropout、LayerNorm、激活、逐元素乘加
**全部仍在 CPU 上**。所以"用了 GPU" ≠ "训练跑在 GPU 上"。

---

## 9. 性能优化：单步 17.2s → 3.85s（4.5×，2026-09-16）

> 起因：任务管理器里 GPU 长期 0%，用户要求"必须优化代码算法"。
> 本节只记"改了什么、实测多少、哪些没用"，原理见 `docs/24-Flash-Attention.md`。

### 9.1 方法：先逐算子插桩，再动手

不猜瓶颈，先在 `src/tensor.rs` 加逐算子计时（`thread_local!` 计数器 + RAII `Guard`，
析构时把纳秒累加到槽位；`prof::take()` 读取并清零）。零原子操作、零锁，
本身开销可忽略，测完删除。

复现命令：

```powershell
cargo build --release
.\target\release\llm_from_scratch.exe train --config config_perf.json *> logs\perf_prof7.log
```

`config_perf.json`：d=128 / 4 层 / 4 头 / batch=8 / block=512，语料 `data/corpus_perf/`（1.74 MB，
免掉 2 分钟 BPE 编码），20 步、`eval_every=99999`。**全程 loss 逐位一致**是每次改完的验收标准
（step 2 = 9.0218、step 20 = 8.5586）。

### 9.2 结果总表

| 版本 | 每步 | tok/s | matmul 占单步 | 日志 |
|---|---|---|---|---|
| 基线 | 17.2 s | 240 | 11%（1.9 s） | `logs\perf_prof.log` |
| ① flash attention 矩阵乘化 | 4.75 s | 886 | 53% | `logs\perf_prof2.log` |
| ② log_softmax 并行化 | 4.55 s | 890 | 58% | `logs\perf_prof3.log` |
| ③ reshape_b / binary_b 并行化 | **3.85 s** | **1049** | 60% | `logs\perf_prof7.log` |

**净提速 4.5×（tok/s 4.4×）**，数值零变化（28/28 测试通过）。

### 9.3 根因：flash attention 是标量三重循环，占单步 78%

插桩第一版就把真相摊开了（`logs\perf_prof.log`，单步 17.1 s）：

```text
[prof] matmul 1.9s/17.1s（11%）| 75 次调用 | 45.1 GFLOP
[prof]   flash_b          8.69s    51%  4 次
[prof]   flash_f          4.58s    27%  4 次
[prof]   log_softmax      0.57s     3%  1 次
```

`flash_attention` 原实现是**分块在线 softmax + 标量三重循环**，有效算力只有
**0.12~0.24 GFLOP/s**——比 `matmul_data`（23 GFLOP/s）慢两个数量级。
这一条直接推翻了本文档 8.8 节的判断（"瓶颈是手写 CPU matmul"）：matmul 只占 11%，
分块也不是没白做。

### 9.4 有效的三项改动

**① `flash_attention` 矩阵乘化**（`src/tensor.rs`，收益最大）

把标量三重循环换成 4 次前向 + 4 次反向矩阵乘：

```text
前向  Q' = Q/√d → S = Q'·Kᵀ → P = softmax(S+M) → O = P·V
反向  dV = Pᵀ·dO → dP = dO·Vᵀ → dS = P ⊙ (dP − Σⱼ dP·P) → dQ = dS·K·scale, dK = dSᵀ·Q'
```

要点：
- 缩放从 `softmax` 内部挪到 `Q` 上（只有 524K 次乘法），于是 `Q'` 可复用给 `dK`；
- 反向 `dP` 必须用 **V**（不是 K）；
- **P 只存归一化后的值**，`dS` 按行用 `P ⊙ (dP − rowsum(dP·P))` 算——
  这条正是当年 flash attention 反向 bug 的根（跨块未同步缩放），
  回归测试 `test_flash_attention_backward_matches_standard` 守着它。

**② `log_softmax_last_dim` 前向 + 反向并行化**（按行 `par_chunks_mut(d)`）

输出头是 `[B*T, vocab]` = 4096×8192 = **3355 万元素**，单线程时占单步 11%（1.05 s）。
反向里 `Ref<Vec<f32>>` 不是 `Sync`，要先把梯度 `rg.borrow().to_vec()` 拷出来再并行写回。

**③ `reshape` 反向 + `binary` 反向并行化**（收益最意外）

`reshape` 的反向是 `sgm[i] += g[i]` 的纯逐元素拷贝，串行时单步 **0.43 s（9%）**；
按 4096 分块并行后降到 **0.075 s/步**。`binary` 反向形状相同时两侧索引都是恒等
（见 `broadcast_plan`），也一并按块并行。

> 教训：矩阵乘之外的**纯访存循环**同样是大头，而且改起来的风险远低于 GEMM 调优。

### 9.5 失败的尝试（都实测量化过，全部已回退）

| 尝试 | 实测 | 结论 |
|---|---|---|
| 寄存器微内核 v1：`MR=4×NR=16`，`acc[i][j]` 动态下标 | matmul 2.5 → 3.5 s/步，整步 4.55 → 5.3 s/步 | **慢 1.4×**（累加器被溢出到栈） |
| 寄存器微内核 v2：改成定长数组 + `iter_mut().zip()` | matmul 2.5 → 3.2 s/步，整步 4.55 → 5.1 s/步 | **仍慢**，收益抵不过寄存器压力与 a 的重复加载 |
| 继续用 GPU 跑（评估 ③） | 99 次 matmul **全走 GPU**（`CPU 部分 0.0s`），整步 3.85 → 5.55 s/步 | **GPU 反而慢 44%** |

结论：LLVM 对"长切片 + 运行时长度"的内层 axpy（`acc_row.iter_mut().zip(b_row)`）
已经自动向量化得不错（23 GFLOP/s）。想再往上只能上 `std::arch` 的显式 AVX2/FMA
intrinsic，属于另一个量级的改动，本轮不做。

### 9.6 GPU（评估 ③）的最终答复

**CPU 版更快，GPU 后端保留但不要默认开。** 原因在 8.8 节已经写明，这里只是用新数据再确认：

- 每次 `gpu::matmul` 都要**上传输入 + 回读输出**（PCIe 往返），一次 dispatch 固定开销 ~10 ms；
  99 次调用光调度就 ~1 s；
- 本配置下所有 matmul 都超过 `MATMUL_MIN_FLOPS = 5000 万`，所以**全部**走 GPU，
  连本该留在 CPU 的小算子也搭上了搬运成本；
- MX150 是入门级低压独显，且着色器是 16×16 分块的教学实现。

换句话说：GPU 要发挥作用，前提是**整个 step 常驻显存、消除每算子往返**，
那是"重写后端"，不是"调个阈值"。

### 9.7 收尾

- **临时插桩已全部删除**（`tensor.rs` 的 `PROF_MM_*` 原子量 + `pub mod prof`、
  `train.rs` 的分段计时与逐算子打印、`rope.rs` 的 2 处 `guard`）。
  需要复现时按 9.1 节重新加回即可；删除后 `cargo test --release` 仍 28/28 通过。
- **保留的有效优化**：`.cargo/config.toml` 的 `target-cpu=native`、
  `Cargo.toml` 的 `lto="thin"`/`codegen-units=1`、matmul 的 MC×NC 分块、
  `transpose_flat` / `reshape_b` / `binary_b` / `log_softmax` 的 rayon 并行化、
  flash attention 的矩阵乘化。
- **不再动 matmul 微内核**：见 9.5 节，LLVM 已把朴素分块版自动向量化到 15 GFLOP/s，
  显式 AVX2/FMA intrinsic 属另一个量级的改动，收益边际。
- **GPU 不默认开启**：需要时 `cargo run --features gpu`，但要接受比 CPU 慢 ~44%。

---

## 附录：本次会话已修复的 bug（供背景理解）

| # | 文件:位置 | 问题 | 影响 |
|---|----------|------|------|
| 1 | `src/tensor.rs:1513` flash_attention 前向 | 在线 softmax 里运行最大值增大时，**已存入 `attn_data` 的旧 P 未随 `rescale` 缩放**，同一行各块用了不同归一化基准，末尾除 `l` 时前序块被放大 `exp(m_final-m_old)` 倍 | **根因**：该因子随注意力变尖锐指数增长 → 反向梯度 1500 步内从 0.67 涨到 5.8e6，权重却几乎不动（前向是对的），所有训练在 step 250 后劣化 |
| 2 | `src/tensor.rs:1602` flash_attention 反向 | `dP_ij = dO_i · V_j` 误写成用 **K**（捕获列表 `src/tensor.rs:1565-1567` 里根本没捕获 `v`） | dP→dS→dQ/dK 全错，实测 dQ 偏差 2.7 倍 |
| 3 | `src/train.rs:440` | 早停 `break` 位于 checkpoint 保存和评估打印**之前** | 日志丢失最后一次评估行；`latest.ckpt` 停在上一周期（实测停在 step 1400 而非 1500） |
| 4 | `src/train.rs:456` | `final.ckpt` 传入 `cfg.steps` 作步数标签 | 早停时标签错误（写 3000，实为 1500） |
| 5 | `src/train.rs:464` | `[done] x s/步` 分母用 `cfg.steps` 而非实际步数 | 每步耗时被系统性低估 |
| 6 | `src/tensor.rs:1796` dropout | 种子从**数据指针 + 首元素**派生，分配器复用地址时同一 dropout 位点每步拿到几乎相同的 mask | dropout 退化成固定掩码（永久丢掉固定通道），正则化失效 |
| 7 | `src/train.rs:342` | grad 日志打印 `min(raw_norm, grad_clip)` | 范数恒等于裁剪阈值，梯度异常完全看不出来（正是它掩盖了 bug 1 长达数小时） |

**回归测试**：`src/tensor.rs` 新增 `test_flash_attention_backward_matches_standard`
（t=16 / bs=4 强制多 K/V 块，**对比梯度**而非输出；旧测试用 `Tensor::from_vec` 不追踪梯度且只有 1 个块，所以漏掉了 bug 1、2）。

已用**变异测试**验证该测试对 bug 1、2 都能捕获（分别注释掉修复后测试均失败：dQ 误差 17.8 / 11.8）。

当前 `cargo test`：**28/28 通过**。

> 为什么 demo 和原有测试没发现这些 bug：`Tensor::flash_attention(&q,&k,&v,&mask, 32)` 的块大小是 32，
> 而 demo 的 `block_size` 也是 32 → 只有 **1 个 K/V 块** → 在线 softmax 的跨块分支从不执行。
> 正式训练 `block_size=256` → 8 个块 → bug 必然触发。

---

## 10. GPU 后端重写：内核吞吐 4×，瓶颈仍是"每算子往返"（2026-09-16）

### 10.1 先量化病根（不改代码，只加插桩）

给 `gpu.rs::run()`（逐算子同步路径）加四段计时，训练首步结束打印一次。`config_perf.json`
（20 步、batch=8、block=512、4 层、vocab=8192）实测：

```
[gpu] dispatch 分解：采样 206 次 | 平均 25.2ms/次，合计 5.18s
[gpu]   操作数上传 0.0ms (0%) | 参数上传 0.0ms (0%) | 绑定+编码+提交 1.8ms (7%) | poll+回读 23.3ms (93%)
[gpu]   回读细分：map_async 0.0ms | poll(Wait) 20.2ms (87%) | get_mapped_range 0.0ms | 拷回 Vec 3.1ms
[gpu]   回读总量 1461.7MB / 4.81s → 304 MB/s
```

三条结论：

1. **时长几乎全在 `poll(Wait)`**，而不是上传、编码或拷回：单次 dispatch 平均 25ms，
   其中 20ms 是 `poll`。`get_mapped_range` 恒为 0、拷回 `Vec` 只占 13%。
2. **一步要回读 1.46 GB**。按形状分组看，最大的三组是注意力 scores（16 次 × 33.6MB）、
   掩码 softmax（8 次 × 33.6MB）、输出头 logits（2 次 × 134MB）。
3. 单独计时（16MB 张量）确认这是**传输带宽**而非固定延迟：不回读时 `poll` 只要 1.1ms，
   附一次 16MB 的 `copy_buffer_to_buffer` 到 `MAP_READ` buffer 后变成 10.8ms
   → 回读带宽约 **1.6 GB/s**（MX150 是 PCIe 3.0 x4，这个数已接近合理上限）。

也就是说：**只要每算一个算子就回读一次，GPU 就必然被 PCIe 往返拖死**，与内核写得好不好无关。

### 10.2 两项修复

**① matmul 内核改寄存器分块**（`matmul_main`）：workgroup 16×16 = 256 线程、
输出 tile 128×128、每线程 8×8 = 64 个累加器。

- 旧版"每线程 1 个元素 + 16×16 共享内存 tile"每次 FMA 要付 2 次 LDS，
  而 LDS 吞吐 1 条/cycle、FMA 4 条/cycle —— 被共享内存卡住 8 倍；
  新版每个 k 步是 16 次 LDS 对 64 次 FMA，两条流水线配平。
- 共享内存取 `[k][row]` / `[k][col]`（pitch = 128），行列都按 stride 16 分配，
  同一 warp 的 16 个线程读连续地址、另 16 个读同地址（广播），无 bank conflict。
- **踩到的坑**：tile 原点一开始写成 `global_invocation_id.x * 128`，但 workgroup 只有
  16 线程宽，`gid.x = wid.x*16 + lid.x`（0..15），于是行距被重复累加 16 次，
  越界 clamp 后所有线程都读到最后一行/列 → 输出几乎全 0。
  必须用 `@builtin(workgroup_id)`。定位办法：写个 dump 内核把共享内存原样倒出来，
  一眼看出 `sh_a[k][127]` 存的是 `A[m-1][k]`。

**② softmax 内核改"一个 workgroup 一行"**（`softmax_fwd_main` / `softmax_bwd_main`）：
256 线程按 stride 256 扫该行（同一 warp 读连续地址、合并成 cache line），
行内最大值/和用共享内存树形归约。旧版"一个线程一行"时同行相邻元素由同一线程串行访问，
每次 load 独占一条 128B 事务 —— 8.4M 元素要 **173ms（≈200 MB/s）**，占整步 1/3。

| 指标 | 修复前 | 修复后 |
|---|---|---|
| matmul 有效算力（批量提交回放） | 23.6 GFLOP/s | **93.2 GFLOP/s** |
| 同卡纯 FMA 峰值（对照） | 524 GFLOP/s | 524 GFLOP/s |
| softmax 8 次 dispatch（每步） | 1383 ms | **444 ms** |
| 训练整步（`config_perf.json`） | 3.85 s | **3.35 s** |
| 训练吞吐 | 1068 tok/s | **1223 tok/s** |

数值一致性：同一配置 CPU 版、GPU 改内核前、GPU 改内核后三次 step-20 都是
`loss 8.5586 | val 8.7149`，逐位相同。

### 10.3 现状与剩余瓶颈

GPU 版只比 CPU 版（4.05 s/步、1049 tok/s）快 ~17%，因为 91% 的 dispatch 时间仍是
`poll(Wait)` 等回读。剩下的账很清楚：

| 形状 | 次数/步 | 回读量 | 耗时 | 说明 |
|---|---|---|---|---|
| 8.4M (网格 4x4x32) | 16 | 536.9 MB | 656 ms | 注意力 scores，只有 512 个 workgroup，占用率低 |
| 8.4M (网格 16384x1x1) | 8 | 268.4 MB | 444 ms | 掩码 softmax（已优化，原 1383ms） |
| 33.6M (网格 32x64x1) | 2 | 268.4 MB | 352 ms | 输出头 logits |
| 0.5M (网格 32x1x1) | 82 | 172.0 MB | 958 ms | 小 matmul：只需 32 个 workgroup，算 2MB 却花 11.7ms |
| 0.5M (网格 4x1x32) | 32 | 67.1 MB | 966 ms | 同上，30ms/次 |

即：**光把中间结果搬回 CPU 就花了 3.75s**（1.46GB / 390MB/s），而这一步的 matmul 计算量
按 93 GFLOP/s 折算只要 ~0.65s。所以下一个数量级的收益只能来自**常驻显存**：
让 scores → 掩码 softmax → attn·V 这条链、以及反向里成串的 matmul 全程留在 GPU 上，
只在真正需要 CPU 语义（损失、优化器、checkpoint）时同步一次。

